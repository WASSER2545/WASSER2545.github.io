<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://wasser2545.github.io/feed.xml" rel="self" type="application/atom+xml"/><link href="https://wasser2545.github.io/" rel="alternate" type="text/html" hreflang="en"/><updated>2026-05-27T07:23:31+00:00</updated><id>https://wasser2545.github.io/feed.xml</id><title type="html">Zhengle Wang</title><subtitle>Ph.D. student at Purdue University working on AI for databases, cloud benchmarking, and data systems. </subtitle><entry><title type="html">Agentic System Workload Optimization</title><link href="https://wasser2545.github.io/blog/2026/agentic-workload/" rel="alternate" type="text/html" title="Agentic System Workload Optimization"/><published>2026-05-23T00:00:00+00:00</published><updated>2026-05-23T00:00:00+00:00</updated><id>https://wasser2545.github.io/blog/2026/agentic-workload</id><content type="html" xml:base="https://wasser2545.github.io/blog/2026/agentic-workload/"><![CDATA[<p><strong>Agentic System Workload Optimization: From Query Plans to Agent-Native Serving</strong></p> <p>The question I care about is simple:</p> <blockquote> <p>As LLM applications move from single calls to multi-step, multi-agent, and tool-using workflows, what exactly should a serving system optimize?</p> </blockquote> <p>My current answer is that the optimization target is shifting from an isolated LLM request to an <strong>agentic workload</strong>: a structured execution process with dependencies, state, repeated sub-computation, heterogeneous resource phases, and bursty downstream effects.</p> <p>This note uses four recent papers to trace that shift:</p> <ol> <li><strong>Halo / Helium</strong> treat agentic workflows as query plans or DAGs.</li> <li><strong>Pythia</strong> treats agentic traffic as a predictable production serving workload.</li> <li><strong>Scepsy</strong> treats agentic execution as aggregate demand over a GPU cluster.</li> </ol> <p>The papers approach the problem from different layers, but they point to the same direction: LLM serving is becoming workflow-aware. At the same time, they also expose a gap that I find more interesting than any single optimization technique: we still do not have a mature macro-level characterization of agentic workloads themselves.</p> <h2 id="1-from-requests-to-workloads">1. From Requests to Workloads</h2> <p>Traditional LLM serving mostly optimizes one request or one model invocation at a time. Continuous batching, PagedAttention, prefix caching, speculative decoding, and KV-cache management are all powerful techniques, but the serving layer usually sees requests as approximately independent. It does not know whether a request is the first planner call in a long workflow, a near-final verifier call, or one branch in a fan-out phase.</p> <p>Agentic workloads break this assumption. A single user task may expand into LLM calls, tool calls, retrieval, SQL/API operations, code execution, and repeated self-correction. These steps have internal dependencies: chains, fan-out/fan-in, loops, tree search, debate, review, and retries. They also create uneven resource phases across GPU prefill/decode, CPU tools, network calls, storage, and context loading.</p> <p>This is why I think the core of agentic system optimization is not simply making one LLM inference faster. It is making agentic execution understandable, predictable, and schedulable as a workload.</p> <h2 id="2-halo-workflow-as-query-plan">2. Halo: Workflow as Query Plan</h2> <p><strong>Halo: <a href="https://arxiv.org/abs/2509.02121">Batch Query Processing and Optimization for Agentic Workflows</a></strong> starts from a database perspective. Its key move is to compile multiple agentic workflows into structured query-plan DAGs, then optimize them as a batch.</p> <p>The intuition is natural: if several workflows share prompts, contexts, tools, or subgraphs, the system should not treat them as unrelated API calls. Once a workflow is represented as a DAG, a processor can reason about common subgraphs, shared computation, CPU/GPU pipelining, cache reuse, and placement.</p> <p>Halo is valuable because it makes an explicit analogy between agentic execution and query processing. It says that orchestration is not enough. If an agent framework only launches steps while the serving system only sees isolated LLM calls, then no layer has a global view of the plan.</p> <p>What this paper suggests to me:</p> <ul> <li>Agentic workflows need a plan-level optimization layer, not just a faster model server.</li> <li>The useful optimization unit may be a batch of structurally related workflows.</li> <li>CPU tool operators and GPU LLM operators should be scheduled together, because pipeline bubbles and resource imbalance happen across that boundary.</li> </ul> <p>The limitation is also clear. Halo works best when workflows have visible and relatively fixed DAGs. Many real agentic systems, especially coding agents and open-ended research agents, do not behave like clean static plans. They replan, retry, pause for tools, and branch based on uncertain intermediate results. So Halo gives a strong abstraction, but it also reveals the cost of assuming too much structure.</p> <h2 id="3-helium-llm-calls-as-first-class-operators">3. Helium: LLM Calls as First-Class Operators</h2> <p><strong>Helium: <a href="https://arxiv.org/abs/2603.16104">Efficient LLM Serving for Agentic Workflows: A Data Systems Perspective</a></strong> extends the same database-oriented line of thought. Its most useful framing is “LLM-as-operator”: LLM invocations should not be hidden inside opaque UDFs. They should be visible to the optimizer.</p> <p>This matters because many LLM-specific optimizations depend on visibility. KV cache, prompt cache, and operator output cache cannot be planned well if the system only sees black-box function calls. Helium uses a DSL/DAG to express batched workflows, then lets the optimizer perform pruning, common subgraph elimination, and cache-aware replacement such as <code class="language-plaintext highlighter-rouge">CacheFetch</code>.</p> <p>I like this paper because it turns caching from a runtime trick into an optimization object. Passive prefix caching relies on accidental similarity. A workflow-aware optimizer can reason about repeated prompt templates, shared prefixes, and reused operator outputs before execution.</p> <p>What this paper suggests to me:</p> <ul> <li>“LLM-as-operator” is a useful systems abstraction for agentic execution.</li> <li>Cache planning should move upward from local runtime policy to workflow-level optimization.</li> <li>Prompt, KV, and operator-output caches may need to be managed together rather than as separate mechanisms.</li> </ul> <p>But Helium shares Halo’s main assumption: the system needs a visible workflow graph and direct access to the serving stack. That may fit controlled platforms, but it is harder in enterprise settings where requests come from mixed frameworks, tools run outside the model server, and APIs hide the full workflow. Recent agent products also seem less dependent on repeated multi-agent debate templates and more dependent on dynamic, tool-heavy execution.</p> <h2 id="4-pythia-workflow-as-predictable-serving-trace">4. Pythia: Workflow as Predictable Serving Trace</h2> <p><strong>Pythia: <a href="https://arxiv.org/abs/2604.25899">Exploiting Workflow Predictability for Efficient Agent-Native LLM Serving</a></strong> feels like a shift from idealized workflow graphs to production serving traces. Instead of assuming that the serving system can fully inspect every workflow DAG, Pythia asks whether lightweight metadata and historical traces are enough to optimize a mixed request stream.</p> <p>In Pythia, each LLM request carries fields such as:</p> <div class="language-json highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">{</span><span class="w">
  </span><span class="nl">"workflow_type_id"</span><span class="p">:</span><span class="w"> </span><span class="s2">"coding_assistant"</span><span class="p">,</span><span class="w">
  </span><span class="nl">"workflow_id"</span><span class="p">:</span><span class="w"> </span><span class="s2">"session_123"</span><span class="p">,</span><span class="w">
  </span><span class="nl">"agent_id"</span><span class="p">:</span><span class="w"> </span><span class="s2">"engineer"</span><span class="w">
</span><span class="p">}</span><span class="w">
</span></code></pre></div></div> <p>The gateway uses historical profiles to attach predicted properties:</p> <div class="language-json highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">{</span><span class="w">
  </span><span class="nl">"predicted_output_len"</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="mi">1000</span><span class="p">,</span><span class="w"> </span><span class="mi">1300</span><span class="p">],</span><span class="w">
  </span><span class="nl">"predicted_path_regex"</span><span class="p">:</span><span class="w"> </span><span class="s2">"planner -&gt; explorer{3,4} -&gt; engineer{3,6} -&gt; reviewer -&gt; verifier"</span><span class="w">
</span><span class="p">}</span><span class="w">
</span></code></pre></div></div> <p>This small amount of metadata changes the serving problem. A router no longer has to balance only by request count. It can route by predicted token/KV pressure. A scheduler no longer has to use FCFS. It can prioritize requests that are close to completing a workflow or likely to unblock an idle downstream model. An autoscaler no longer has to react after a queue has already exploded. It can look ahead along predicted workflow phases.</p> <p>The paper’s production traces highlight three important workload properties:</p> <ul> <li>Prefix reuse is fragile because agent prompts vary and tool gaps can evict useful cache entries.</li> <li>Resource footprints differ sharply across roles such as planner, engineer, reviewer, and verifier.</li> <li>Bursts propagate through the workflow graph, so upstream phases can create downstream queue spikes.</li> </ul> <p>A simple coding-agent example makes the point:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>planner -&gt; explorer -&gt; engineer -&gt; reviewer -&gt; verifier
</code></pre></div></div> <p>Traditional serving may stack several long engineer requests on one GPU, delay short verifier requests, and start scaling only after the burst has moved to another phase. Pythia instead uses predicted output length, workflow position, downstream idle risk, and phase lookahead to route, prioritize, preempt, and scale more deliberately.</p> <p>What this paper suggests to me:</p> <ul> <li>Even partial workflow metadata can be extremely valuable.</li> <li>Workload predictability does not require a perfect static DAG; statistical profiles may be enough.</li> <li>The serving layer should distinguish agent roles, not just models and prompts.</li> </ul> <p>This is the closest of the four papers to a real workload perspective. Its limitation is that it depends on platform observability and metadata interfaces. If third-party agents do not expose workflow IDs, agent roles, or path hints, the serving layer has to infer structure from much weaker signals. The paper also leaves open how stable these profiles remain under high request variety.</p> <h2 id="5-scepsy-workflow-as-aggregate-cluster-demand">5. Scepsy: Workflow as Aggregate Cluster Demand</h2> <p><strong>Scepsy: <a href="https://arxiv.org/abs/2604.15186">Serving Agentic Workflows Using Aggregate LLM Pipelines</a></strong> looks at the problem from the cluster allocation side. Agentic workflows may call multiple LLMs, and the number of models can exceed the number of available GPUs. Exact execution is hard to predict because workflows branch, loop, and generate variable-length outputs. But Scepsy observes that each LLM’s aggregate share of total execution time can still be stable enough for resource allocation.</p> <p>Its core idea is to collect low-level LLM invocation traces, build an Aggregate LLM Pipeline, and search over allocation choices such as replica count, tensor parallel degree, fractional GPU share, and topology-aware placement.</p> <p>Compared with Pythia, Scepsy is less about online scheduling for each request and more about offline or periodic cluster configuration. It asks: given an agentic workload with multiple models, how should limited GPU resources be divided?</p> <p>What this paper suggests to me:</p> <ul> <li>Not all useful workload structure has to be graph-level structure.</li> <li>Aggregate per-model demand can be enough for cluster-level optimization.</li> <li>For arbitrary agent frameworks, low-level invocation traces may be easier to collect than complete workflow DAGs.</li> </ul> <p>The main caveat is that Scepsy assumes tool and orchestration time is small. That may not hold for tool-heavy agents, browser agents, data analysts, or coding agents that spend substantial time outside the LLM server.</p> <h2 id="6-the-shared-direction">6. The Shared Direction</h2> <p>These four papers operate at different layers, but I read them as parts of the same transition:</p> <table> <thead> <tr> <th>Layer</th> <th>Paper</th> <th>Optimization target</th> </tr> </thead> <tbody> <tr> <td>Query plan / batch workflow</td> <td>Halo</td> <td>Consolidated workflow DAG, CPU-GPU scheduling</td> </tr> <tr> <td>Query optimizer / cache</td> <td>Helium</td> <td>LLM-as-operator, proactive KV/prompt cache</td> </tr> <tr> <td>Serving runtime / production trace</td> <td>Pythia</td> <td>Workflow predictability, lookahead scheduling, autoscaling</td> </tr> <tr> <td>Cluster allocation</td> <td>Scepsy</td> <td>Multi-LLM GPU allocation, fractional GPU placement</td> </tr> </tbody> </table> <p>Halo and Helium start from system abstraction: if the workflow graph is visible, optimize it like a query plan. Pythia starts from production traces: if workflows are statistically predictable, use that predictability in the serving runtime. Scepsy starts from GPU clusters: if exact execution is dynamic, optimize around aggregate model demand.</p> <p>The common claim is stronger than any one paper: the serving system should no longer treat agentic requests as independent model calls.</p> <h2 id="7-the-missing-layer-macro-workload-characterization">7. The Missing Layer: Macro Workload Characterization</h2> <p>The more I read these papers, the more I feel that the missing piece is not another isolated scheduler. It is a systematic workload study for agentic systems.</p> <p>Current papers often use workload analysis as motivation rather than as the main contribution. Their benchmarks and workflow patterns are either synthetic, platform-specific, or locally realistic. We still lack something comparable to classic production workload studies in databases and cloud systems: cross-application, cross-industry, cross-platform measurements that tell us what agentic workloads actually look like.</p> <p>Some reasons are understandable:</p> <ul> <li>Real agent traces are sensitive. User inputs, enterprise documents, code, tool calls, API responses, and intermediate reasoning may all involve privacy concerns.</li> <li>Agentic applications are evolving quickly. Coding agents, deep research agents, data analysts, browser agents, and customer support agents may have very different workload shapes.</li> <li>Systems papers need controlled evaluation, so fixed DAGs and synthetic patterns are easier to benchmark.</li> </ul> <p>But without macro characterization, many optimization assumptions remain uncertain. For example:</p> <ul> <li>What is the graph-shape distribution of mainstream agentic workloads?</li> <li>How much time is spent in LLM inference, tools, orchestration, network calls, and human waiting?</li> <li>How much real prefix/KV/tool-result reuse exists across users or sessions?</li> <li>How often do workflows replan, retry, fail, branch, or self-correct?</li> <li>Which agent roles dominate latency, cost, KV pressure, and queueing delay?</li> </ul> <p>These questions matter because they decide which optimizations are worth building. If workflows are mostly fixed and repeated, Halo/Helium-style plan optimization becomes very attractive. If workflows are dynamic but statistically predictable, Pythia-style metadata and profiling become more important. If exact graph structure is unavailable but model-level demand is stable, Scepsy-style aggregate allocation may be the practical path.</p> <h2 id="8-my-current-hypothesis">8. My Current Hypothesis</h2> <p>My current hypothesis is that agentic infrastructure will evolve in a direction similar to database systems: from local execution tricks toward workload-aware resource management.</p> <p>In databases, the important abstraction was not just a faster operator. It was the query plan, the optimizer, the workload, and eventually the separation of storage, compute, and pricing models around different workload classes. Agentic systems may follow a similar path. The infrastructure will need to understand plans when they are visible, infer profiles when they are not, and allocate resources based on workload phases rather than isolated calls.</p> <p>This also suggests a business-side shift. Enterprise agentic workloads, especially large-scale software engineering tasks, look increasingly different from consumer chat traffic. They demand longer context, larger KV capacity, more tool execution, more retries, and stronger guarantees around latency and completion. I expect this divergence to push agentic infrastructure companies toward OLAP-like pricing and resource-management models, where users pay not only for tokens but for long-running, stateful, workflow-level compute.</p> <p>So my takeaway from these papers is not simply that agentic serving needs better batching or caching. It is that the field needs to define the workload itself. Once we know what agentic workloads really look like, the right optimizers, schedulers, cache hierarchies, and pricing models become much easier to reason about.</p>]]></content><author><name></name></author><category term="research"/><category term="agentic-systems"/><category term="LLM-serving"/><category term="workload-optimization"/><summary type="html"><![CDATA[From query-plan optimization to agent-native serving, and why agentic systems need macro workload characterization.]]></summary><media:thumbnail xmlns:media="http://search.yahoo.com/mrss/" url="https://wasser2545.github.io/assets/img/blog/agentic-workload-preview.png"/><media:content medium="image" url="https://wasser2545.github.io/assets/img/blog/agentic-workload-preview.png" xmlns:media="http://search.yahoo.com/mrss/"/></entry></feed>