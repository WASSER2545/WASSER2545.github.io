<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://wasser2545.github.io/feed.xml" rel="self" type="application/atom+xml"/><link href="https://wasser2545.github.io/" rel="alternate" type="text/html" hreflang="en"/><updated>2026-05-26T18:24:59+00:00</updated><id>https://wasser2545.github.io/feed.xml</id><title type="html">Zhengle Wang</title><subtitle>Ph.D. student at Purdue University working on AI for databases, cloud benchmarking, and data systems. </subtitle><entry><title type="html">Agentic System Workload Optimization</title><link href="https://wasser2545.github.io/blog/2026/agentic-workload/" rel="alternate" type="text/html" title="Agentic System Workload Optimization"/><published>2026-05-23T00:00:00+00:00</published><updated>2026-05-23T00:00:00+00:00</updated><id>https://wasser2545.github.io/blog/2026/agentic-workload</id><content type="html" xml:base="https://wasser2545.github.io/blog/2026/agentic-workload/"><![CDATA[<p><strong>Agentic System Workload Optimization: From Query Plans to Agent-Native Serving</strong></p> <p>The core question:</p> <blockquote> <p>As LLM applications move from single requests to multi-step, multi-agent, and tool-using workflows, the object of system optimization is no longer a single LLM call. It becomes an agentic workload with structure, state, repetition, and heterogeneous resource phases.</p> </blockquote> <p>The main thread of this reading note:</p> <ol> <li><strong>Halo / Helium</strong>: model agentic workflows as query plans or DAGs from a database and query processing perspective.</li> <li><strong>Pythia</strong>: use workflow predictability for proactive runtime optimization from the perspective of production multi-agent serving.</li> <li><strong>Scepsy</strong>: allocate resources for arbitrary multi-LLM workflows from the perspective of GPU clusters.</li> </ol> <p>My expected takeaways:</p> <ul> <li>The common trend across these papers is a shift from request-level LLM serving toward workflow-level optimization.</li> <li>However, these papers still do not provide enough macro-level characterization of what the agentic workload itself looks like. Workload characterization remains an open problem.</li> </ul> <h2 id="1-background-why-agentic-workloads-need-new-system-optimization">1. Background: Why Agentic Workloads Need New System Optimization</h2> <h3 id="11-the-optimization-target-in-traditional-llm-serving">1.1 The Optimization Target in Traditional LLM Serving</h3> <ul> <li>A single request or a single model invocation.</li> <li>Typical optimizations include continuous batching, PagedAttention, prefix caching, speculative decoding, and KV cache management.</li> <li>The default assumption is that requests are approximately independent, and the serving layer does not know the structure of the upper-level workflow.</li> </ul> <h3 id="12-what-changes-in-agentic-workloads">1.2 What Changes in Agentic Workloads</h3> <ul> <li>A user task expands into multiple LLM calls, tool calls, retrieval/API/SQL operations, or code execution steps.</li> <li>The workflow has internal dependencies: chains, fan-out/fan-in, loops, tree search, or multi-agent debate.</li> <li>Across workflows or batches, there may be repeated work: shared prompts, shared prefixes, shared tool results, or shared subgraphs.</li> <li>Resource phases are uneven: GPU prefill/decode, CPU-side tools, network/API calls, and storage/context loading.</li> <li>Traffic may contain structured bursts: requests from an upstream agent can trigger cascades of downstream agent requests.</li> </ul> <h3 id="13-the-vision-for-agentic-workload-optimization">1.3 The Vision for Agentic Workload Optimization</h3> <blockquote> <p>The core of agentic system optimization is not making a single LLM inference faster. It is making agentic execution understandable, predictable, and schedulable as a system workload.</p> </blockquote> <h2 id="2-paper-1-halo">2. Paper 1: Halo</h2> <h3 id="21-basic-information">2.1 Basic Information</h3> <p>Title: <strong>Batch Query Processing and Optimization for Agentic Workflows</strong></p> <p>Authors: Junyi Shen, Noppanat Wadlom, Yao Lu</p> <p>Affiliation: National University of Singapore, Singapore</p> <p>Version: arXiv:2509.02121v2, January 19, 2026</p> <h3 id="22-topic">2.2 Topic</h3> <p>Topic: <strong>Batch query processing for agentic workflows</strong></p> <ul> <li>Multiple agentic workflows can be viewed as a batch of structurally similar queries.</li> <li>A workflow DAG exposes opportunities for shared computation, shared prompts/context, and shared CPU/GPU execution.</li> <li>Agentic workflows need plan-level optimization, similar to batch query processing in databases.</li> </ul> <h3 id="23-insights">2.3 Insights</h3> <ul> <li>Existing LLM serving engines optimize only individual calls and ignore the workflow DAG.</li> <li>Existing agent frameworks handle orchestration, but not system-level performance planning.</li> <li>CPU tool operators and GPU LLM operators execute together, creating pipeline bubbles and low resource utilization.</li> </ul> <h3 id="24-core-idea">2.4 Core Idea</h3> <ul> <li>Compile each workflow into a structured query-plan DAG.</li> <li>Build a consolidated graph for batch queries to expose shared computation.</li> <li>Use a cost model that jointly considers heterogeneous resources, prefill/decode cost, cache reuse, and GPU placement.</li> <li>Let the processor handle adaptive batching, KV-cache sharing/migration, and CPU-GPU pipelining.</li> </ul> <h3 id="25-evaluation">2.5 Evaluation</h3> <p>Strengths:</p> <ul> <li>It directly transfers ideas from database query optimization to agentic workflows.</li> <li>It considers both CPU tools and GPU LLMs, giving the system design a relatively complete view.</li> </ul> <p>Limitations:</p> <ul> <li>The optimized tasks are all fixed-DAG workflows. Real agentic systems usually do not have such idealized reasoning structures, especially for open-ended tasks such as programming.</li> <li>Halo’s experimental environment is relatively white-box and assumes a single machine with multiple GPUs. This does not fully match many current agent products, where APIs and serving run in the cloud while tools execute locally.</li> </ul> <h2 id="3-paper-2-helium">3. Paper 2: Helium</h2> <h3 id="31-basic-information">3.1 Basic Information</h3> <p>Title: <strong>Efficient LLM Serving for Agentic Workflows: A Data Systems Perspective (Extended)</strong></p> <p>Authors: Noppanat Wadlom, Junyi Shen, Yao Lu</p> <p>Affiliation: National University of Singapore, Singapore</p> <p>Version: arXiv:2603.16104v1, March 17, 2026</p> <h3 id="32-topic">3.2 Topic</h3> <p>Topic: <strong>Workflow-aware LLM serving with proactive caching and cache-aware scheduling</strong></p> <ul> <li>Agentic workflows should be modeled as query plans.</li> <li>LLM invocations should be first-class operators, not black boxes wrapped inside UDFs.</li> <li>KV cache, prompt cache, and operator output cache should all be visible to the optimizer.</li> </ul> <h3 id="33-problems">3.3 Problems</h3> <ul> <li>Traditional data systems wrap LLM calls as UDFs, which prevents the system from using LLM-specific optimizations.</li> <li>Traditional LLM serving sees only a single call and cannot exploit inter-operator or inter-workflow sharing.</li> <li>Passive prefix caching relies on luck and cannot use the structure of batched workflows.</li> </ul> <h3 id="34-core-idea">3.4 Core Idea</h3> <ul> <li>Use a DSL/DAG to express batched agentic workflows.</li> <li>Let the query optimizer perform pruning, common subgraph elimination, and replacement with <code class="language-plaintext highlighter-rouge">CacheFetch</code>.</li> <li>Use a templated radix tree to capture prompt prefix structure.</li> <li>Proactively warm static prompt prefixes and use prompt cache to skip repeated operators.</li> </ul> <h3 id="35-evaluation">3.5 Evaluation</h3> <p>Strengths:</p> <ul> <li>The paper explains “LLM-as-operator” very clearly, which is useful for building a theoretical framework.</li> <li>It elevates caching from a runtime trick into an object of query optimization.</li> </ul> <p>Limitations:</p> <ul> <li>It has the same issue as Halo: the method strongly depends on fixed-DAG workflows and requires direct access to the GPU server.</li> <li>In real enterprise settings with mixed request streams on shared clusters, this caching strategy may be hard to use effectively because the system cannot observe the complete workflow graph.</li> <li>Recent agentic system designs rarely rely on the kind of multi-agent debate pattern with shared prefixes/prompts assumed by these two papers. Mature use cases are often more open-ended and include interruptions and retries, which differ substantially from the fixed-DAG assumption.</li> </ul> <h2 id="4-paper-3-pythia">4. Paper 3: Pythia</h2> <h3 id="41-basic-information">4.1 Basic Information</h3> <p>Title: <strong>Pythia: Exploiting Workflow Predictability for Efficient Agent-Native LLM Serving</strong></p> <p>Authors: Shan Yu, Junyi Shu, Yuanjiang Ni, Kun Qian, Xue Li, Yang Wang, Jinyuan Zhang, Ziyi Xu, Shuo Yang, Lingjun Zhu, Ennan Zhai, Qingda Lu, Jiarong Xing, Youyou Lu, Xin Jin, Xuanzhe Liu, Harry Xu</p> <p>Affiliations: UCLA, Alibaba Cloud Computing, Alibaba Group, Intel, SJTU, UC Berkeley, Rice University, Tsinghua University, Peking University</p> <p>Version: arXiv:2604.25899v2, May 14, 2026</p> <h3 id="42-topic">4.2 Topic</h3> <p>Topic: <strong>Predictability-driven agent-native LLM serving</strong></p> <p>This paper is not primarily about static DAG optimization for each workflow. Instead, in a mixed request stream, each request is one LLM call from some workflow. The serving layer receives only lightweight metadata, learns workflow statistics from historical traces, and uses those patterns to optimize caching, routing, priority, and autoscaling.</p> <p>Example workflow:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>A -&gt; B -&gt; C -&gt; D
id: 123
A: planner
</code></pre></div></div> <h3 id="43-problems">4.3 Problems</h3> <p>Pythia’s production traces reveal three problems:</p> <ul> <li>Low prefix cache hit rate: different agent prompts vary significantly, and tool gaps cause cache entries to be evicted from GPU L1/L2 caches in the serving cluster.</li> <li>Severe resource contention: long-context requests are mixed together, causing load imbalance, preemption, and recomputation. For example, a batch may contain both long-context requests and lightweight requests, and the long-context requests can occupy the cluster’s KV cache. Inside the same workflow, Planner, Engineer, Reviewer, and Verifier also have different resource footprints. If the system does not know the agent role, early long-output requests may occupy resources and block near-complete Verifier/Reviewer requests.</li> <li>Structured bursts are obvious: the workflow graph propagates bursts from upstream agents to downstream agents.</li> </ul> <h3 id="44-core-design">4.4 Core Design</h3> <p><strong>0. Add predictive information</strong></p> <p>Originally, each LLM API request contains only information such as prompt and model. Pythia requires the agent framework to attach three lightweight fields:</p> <div class="language-json highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">{</span><span class="w">
  </span><span class="nl">"workflow_type_id"</span><span class="p">:</span><span class="w"> </span><span class="s2">"coding_assistant"</span><span class="p">,</span><span class="w">
  </span><span class="nl">"workflow_id"</span><span class="p">:</span><span class="w"> </span><span class="s2">"session_123"</span><span class="p">,</span><span class="w">
  </span><span class="nl">"agent_id"</span><span class="p">:</span><span class="w"> </span><span class="s2">"engineer"</span><span class="w">
</span><span class="p">}</span><span class="w">
</span></code></pre></div></div> <p>The Workflow Profiler in the gateway looks up the statistical profile for this agent role from historical traces, then injects:</p> <div class="language-json highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">{</span><span class="w">
  </span><span class="nl">"predicted_output_len"</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="mi">1000</span><span class="p">,</span><span class="w"> </span><span class="mi">1300</span><span class="p">],</span><span class="w">
  </span><span class="nl">"predicted_path_regex"</span><span class="p">:</span><span class="w"> </span><span class="s2">"planner -&gt; explorer{3,4} -&gt; engineer{3,6} -&gt; reviewer -&gt; verifier"</span><span class="p">,</span><span class="w">
  </span><span class="nl">"prompt_composition"</span><span class="p">:</span><span class="w"> </span><span class="p">{}</span><span class="w">
</span><span class="p">}</span><span class="w">
</span></code></pre></div></div> <p>All later policies depend on these predicted fields.</p> <p><strong>1. Resource-aware routing: balance by token/KV upper bounds instead of request count</strong></p> <p>A normal router might do this:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Send the request to the replica with the shortest queue.
</code></pre></div></div> <p>The problem is that two queues with the same number of requests may impose very different resource pressure:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>GPU0: 2 Engineers, each expected to generate 3000 tokens
GPU1: 2 Planners, each expected to generate 60 tokens
</code></pre></div></div> <p>Pythia changes this to statistical capacity routing. For each request, the profiler provides a high-confidence upper bound for output length:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>u_i = 99th percentile predicted output length
</code></pre></div></div> <p>For each candidate GPU replica, Pythia checks whether adding the new request would keep the sum of active requests’ token upper bounds within KV capacity:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>sum(u_i) &lt;= C
</code></pre></div></div> <p>If the condition holds, the replica is considered statistically safe. More formally, Pythia uses a union bound to control the probability of OOM:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>P_oom &lt;= sum(alpha_i)
</code></pre></div></div> <p>That is, if each request exceeds its own upper bound with probability <code class="language-plaintext highlighter-rouge">alpha_i</code>, the probability that all requests together exceed memory is controlled under a threshold <code class="language-plaintext highlighter-rouge">epsilon</code>.</p> <p>Among all safe replicas, Pythia chooses the one with the largest expected headroom:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>headroom = capacity - expected_active_size
</code></pre></div></div> <p>If several replicas are similar, cache affinity is used as a tie-breaker. For example, a worker that already has related L2 cache entries for the same workflow is preferred.</p> <p>The result is that long-output or long-context requests are not blindly stacked onto the same GPU, reducing OOM, preemption, and head-of-line blocking.</p> <p><strong>2. Graph-driven priority: prioritize by workflow position and downstream idle risk, not FCFS</strong></p> <p>A normal local scheduler often uses FCFS:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Run whichever request arrives first.
</code></pre></div></div> <p>Pythia computes a <code class="language-plaintext highlighter-rouge">base_priority</code> for each request:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>base_priority =
  w1 * completion_score
+ w2 * unblock_score
</code></pre></div></div> <p>The first term is <strong>completion_score</strong>:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>completion_score = 1 / E[remaining_distance]
</code></pre></div></div> <p>The idea is that requests closer to the end of a workflow get higher priority. For example:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>planner -&gt; engineer -&gt; reviewer -&gt; verifier
</code></pre></div></div> <p>If <code class="language-plaintext highlighter-rouge">verifier</code> is close to completing the whole workflow, it should run before a newly started <code class="language-plaintext highlighter-rouge">planner</code>, because finishing it can release the entire job and reduce job completion time.</p> <p>The second term is <strong>unblock_score</strong>, which asks whether this request will unlock an idle downstream model.</p> <p>Pythia scans the future path:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>future_agents = GetFutureAgents(predicted_path_regex)
</code></pre></div></div> <p>If a future agent’s model replica currently has a short queue and is about to become idle, then the upstream request that will soon produce its input receives a priority boost. The closer that downstream agent is, the larger the boost:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>unblock_score += 1 / expected_distance_to_that_agent
</code></pre></div></div> <p>For example:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>engineer -&gt; reviewer
</code></pre></div></div> <p>If the reviewer model is nearly idle and engineer completion will trigger reviewer immediately, the engineer request receives higher priority. This prevents downstream GPUs from waiting idly.</p> <p><strong>3. Local scheduler: reorder at every iteration and add aging to prevent starvation</strong></p> <p>After a request reaches a worker, it does not run forever in a fixed order. At the start of each scheduling window or iteration, Pythia recomputes dynamic priority:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>dynamic_priority = base_priority + aging(wait_time)
</code></pre></div></div> <p>Then it selects the highest-priority batch that fits in memory.</p> <p>Aging is important. If the scheduler only favors near-completion requests, early-stage planners or explorers may starve. Aging gradually increases priority with wait time.</p> <p>This creates a tradeoff:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Finish workflows that are near completion as soon as possible.
Avoid permanently queueing early-stage requests.
</code></pre></div></div> <p><strong>4. Priority-aware preemption: when memory is tight, evict low-priority requests</strong></p> <p>In LLM serving, if some requests generate more tokens than predicted, KV memory may suddenly become tight. Traditional systems may preempt requests by FCFS or another simple rule and return the preempted request to the back of the queue.</p> <p>Pythia instead does this:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>When worker memory pressure is high:
  Find the active request with the lowest dynamic_priority.
  Pause or evict it.
  Put it back into the local queue.
</code></pre></div></div> <p>The evicted request is usually:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>an early-stage request,
far from completion,
not unlocking downstream work,
currently low priority.
</code></pre></div></div> <p>It is usually not:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>a near-complete verifier,
an engineer that unlocks an idle reviewer,
or another request on the workflow critical path.
</code></pre></div></div> <p>This reduces waste at the workflow level. Even if local preemption happens, Pythia tries not to interrupt the critical path.</p> <p><strong>5. Phase-adaptive autoscaling: scale model replicas ahead of workflow phases</strong></p> <p>This part handles bursts and inter-model resource contention.</p> <p>Pythia projects all active requests’ <code class="language-plaintext highlighter-rouge">predicted_path_regex</code> over a lookahead horizon:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>imminent_agents = ProjectGraph(regex, H)
</code></pre></div></div> <p>It then estimates the near-future load for each model:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>D[agent.model] += EstimatedLoad(agent)
</code></pre></div></div> <p>Next, it estimates the required number of replicas:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>R'[model] = EstimateReplicas(D[model])
</code></pre></div></div> <p>If demand for a model is expected to rise, Pythia scales it up ahead of time:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>planner -&gt; explorer{10}
</code></pre></div></div> <p>When the system sees 50 planners running, it knows that 500 explorer requests may arrive soon, so it can load explorer model replicas in advance.</p> <p>If a model will not be needed within the lookahead horizon, Pythia scales it down:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>The planner phase has passed, and no future request will call the planner model.
</code></pre></div></div> <p>Pythia first tells the scheduler to stop assigning new requests to those replicas, allowing them to drain quickly. Once their queues are empty, resources are released instead of waiting for a fixed keep-alive timeout.</p> <p>This avoids the lag of reactive autoscaling and prevents cold starts and incorrect cache eviction.</p> <p><strong>Putting the pieces together</strong></p> <p>Assume 30 coding assistant workflows run concurrently:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>planner -&gt; explorer -&gt; engineer -&gt; reviewer -&gt; verifier
</code></pre></div></div> <p>Traditional serving:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>1. Planner/explorer fan-out suddenly bursts and queues explode.
2. The router balances by request count and stacks several long engineer requests on one GPU.
3. Reviewer/verifier requests wait for a long time, even though they are short.
4. A GPU's KV memory overflows, causing random preemption and recomputation.
5. The autoscaler starts scaling when the engineer queue is already overloaded, but by then the burst has moved to reviewer.
</code></pre></div></div> <p>Pythia:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>1. It predicts explorer fan-out after planner through the regex and scales explorer replicas early.
2. Engineer requests are routed to GPUs with enough KV headroom using predicted_output_len.
3. Reviewer/verifier requests get higher priority because they are close to the workflow end.
4. If reviewer GPUs are about to become idle, engineer requests that unlock reviewers are accelerated.
5. When memory overflows, Pythia pauses low-priority early-stage requests instead of interrupting the critical path.
6. After a phase ends, it quickly scales down models that are no longer needed.
</code></pre></div></div> <h3 id="45-evaluation">4.5 Evaluation</h3> <p>Strengths:</p> <ul> <li>It is closest to a real workload perspective and includes production traces.</li> <li>It is a solid systems paper that shows effective optimization strategies once a workload profile is available.</li> </ul> <p>Limitations:</p> <ul> <li>It depends on platform observability and metadata interfaces, which may be difficult for open third-party agents.</li> <li>Its workload analysis and experiments are still limited. In real scenarios, high request variety may affect prediction quality.</li> </ul> <h2 id="5-paper-4-scepsy">5. Paper 4: Scepsy</h2> <h3 id="51-basic-information">5.1 Basic Information</h3> <p>Title: <strong>Scepsy: Serving Agentic Workflows Using Aggregate LLM Pipelines</strong></p> <p>Authors: Marcel Wagenländer, Otto White, Britannio Jarrett, Guo Li, Yanda Tao, Huanzhou Zhu, Llúis Vilanova, Pedro Silvestre, Peter Pietzuch</p> <p>Affiliations: Imperial College London; Independent Researcher</p> <p>Version: arXiv:2604.15186v1, April 16, 2026</p> <h3 id="52-topic">5.2 Topic</h3> <p>Topic: <strong>GPU allocation for arbitrary multi-LLM agentic workflows</strong></p> <ul> <li>Agentic workflows may contain multiple LLMs, and the number of LLMs often exceeds the number of available GPUs.</li> <li>End-to-end latency is affected by branching, fan-out, loops, and token generation, making it difficult to predict precisely.</li> <li>However, each LLM’s aggregate share of total execution time is relatively stable and can be used for resource allocation.</li> </ul> <h3 id="53-problems">5.3 Problems</h3> <ul> <li>Arbitrary agentic programs: different frameworks such as LangChain, AutoGen, and Camel.</li> <li>Unpredictable execution: workflows can include data-dependent branching, fan-out, and recursion.</li> <li>Conflicting objectives: throughput and latency create tradeoffs.</li> <li>GPU oversubscription: multiple LLMs share limited GPUs, and manual allocation is often inefficient.</li> </ul> <h3 id="54-core-idea">5.4 Core Idea</h3> <ul> <li>Collect aggregate per-LLM statistics from low-level LLM invocation traces.</li> <li>Build an Aggregate LLM Pipeline to predict latency and throughput under different allocations.</li> <li>Search over GPU allocation choices: replica count, tensor parallel degree, and fractional GPU share.</li> <li>Use topology-aware placement to reduce fragmentation while respecting NVLink/network topology.</li> </ul> <h3 id="55-evaluation">5.5 Evaluation</h3> <p>Scepsy is conceptually similar to Pythia: both use statistical features from request streams for resource scheduling. But Scepsy is more focused on offline configuration and scheduling, similar to knob tuning.</p> <p>Strengths:</p> <ul> <li>It covers multi-LLM and GPU cluster allocation, which Halo and Helium emphasize less.</li> <li>It does not strongly depend on a single agent framework, so its engineering adaptation surface is broader.</li> </ul> <p>Limitations:</p> <ul> <li>It assumes that tool and orchestration time is small, which may not hold for tool-heavy agent workloads.</li> </ul> <h2 id="6-relationship-among-the-four-papers">6. Relationship Among the Four Papers</h2> <table> <thead> <tr> <th>Layer</th> <th>Paper</th> <th>Optimization target</th> </tr> </thead> <tbody> <tr> <td>Query plan / batch workflow</td> <td>Halo</td> <td>consolidated workflow DAG, CPU-GPU scheduling</td> </tr> <tr> <td>Query optimizer / cache</td> <td>Helium</td> <td>LLM-as-operator, proactive KV/prompt cache</td> </tr> <tr> <td>Serving runtime / production trace</td> <td>Pythia</td> <td>workflow predictability, lookahead scheduling, autoscaling</td> </tr> <tr> <td>Cluster allocation</td> <td>Scepsy</td> <td>multi-LLM GPU allocation, fractional GPU placement</td> </tr> </tbody> </table> <ul> <li>Halo/Helium start from system abstractions and assume that the workflow DAG is visible.</li> <li>Pythia starts from production traces and shows that agentic traffic has structure and bursts.</li> <li>Scepsy starts from GPU clusters and describes workload through aggregate LLM demand.</li> </ul> <h2 id="7-macro-workload-critique">7. Macro Workload Critique</h2> <p>Current limitations in these papers:</p> <ul> <li>Workload analysis is often motivation rather than an independent contribution.</li> <li>Benchmarks and workflow patterns are synthetic or only locally realistic, lacking cross-application, cross-industry, and cross-platform statistics.</li> <li>There is no macro-level analysis comparable to production workload studies in the database community.</li> </ul> <p>Possible reasons:</p> <ul> <li>Real agent traces are highly sensitive: user input, enterprise documents, code, tool calls, and API responses may all involve privacy concerns.</li> <li>Agentic applications are still evolving quickly. Coding agents, deep research, data analysts, browser agents, and customer support agents can have very different workloads.</li> <li>For evaluability, systems papers tend to prefer fixed DAGs, fixed patterns, and controlled benchmarks.</li> </ul> <p>Open questions:</p> <ul> <li>What is the graph shape distribution of mainstream agentic workloads?</li> <li>What is the ratio among LLM, tool, and operator execution?</li> <li>How much real prefix/KV/tool-result reuse opportunity exists?</li> <li>How much do tool latency, network latency, and human-in-the-loop latency contribute to end-to-end time?</li> <li>How much do dynamic replanning, failure/retry, and self-correction affect system scheduling?</li> </ul> <h2 id="8-industry-notes">8. Industry Notes</h2> <p>I recently discussed agentic workload testing and benchmarking with people in industry.</p> <ol> <li>Traditional replay is extremely difficult and expensive in agentic systems. At ByteDance, for example, they record intent information and device information to help reproduce behavior.</li> <li>Agentic system providers each have their own internal performance benchmarks, such as coding tasks. However, because these benchmarks involve business secrets, they are unlikely to be released.</li> </ol> <h2 id="9-my-thoughts">9. My Thoughts</h2> <p>Based on current signals, I think agentic system serving is increasingly moving along a path similar to the commercialization of database systems. For example, vLLM recently designed a Pageflow framework to separate the KV cache layer from the compute layer and organize it into three storage tiers. This design resembles Snowflake-style separation of storage and compute.</p> <p>At the same time, as large internet companies increasingly care about the ability of agentic systems to write code for large projects, enterprise requests are quickly diverging from ordinary consumer requests. These requests impose rapidly growing demands on context length and KV cache. I believe this will accelerate the movement of agentic companies toward OLAP-like pricing and business models.</p>]]></content><author><name></name></author><category term="research"/><category term="agentic-systems"/><category term="LLM-serving"/><category term="workload-optimization"/><summary type="html"><![CDATA[Notes on workflow-level optimization for agentic systems, from query plans to agent-native serving.]]></summary></entry></feed>